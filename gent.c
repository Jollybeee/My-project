#include <stdio.h>
#include "hh.h"


int main(void){
    char z; // decrtion of variable
    z = slack('a', 'g'); //performs a mod division and return the rem in char
    printf(z);
    return (0);
}